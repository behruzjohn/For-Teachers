import styled from '@emotion/styled';

export const StyleHeader = styled.div`
  .header_links-li {
    color: black;
    text-decoration: none;
    font-size: 15px;
    list-style: none;
    a {
      color: black;
      text-decoration: none;
    }
  }
  .header {
    min-height: 60px;
  }
  .header-title {
    margin-left: 3px;
    font-size: 18px;
  }
  .profile {
    cursor: pointer;
  }
`;
